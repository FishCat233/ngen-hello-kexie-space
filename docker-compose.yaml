version: '3.8'

services:
  ngen-hello-kexie-space:
    image: ghcr.io/fishcat233/ngen-hello-kexie-space:latest
    container_name: ngen-hello-kexie-space
    ports:
      - "8080:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
